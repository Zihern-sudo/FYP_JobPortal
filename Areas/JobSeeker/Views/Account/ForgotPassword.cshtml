@{
    ViewData["Title"] = "Forgot Password";
    Layout = null;
}

<link rel="stylesheet" href="~/css/auth.css" />
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<div class="auth-container">
    <h1>Forgot Password</h1>
    <p>Please enter your registered email to reset your password.</p>

    <form asp-area="JobSeeker" asp-controller="Account" asp-action="ForgotPassword" method="post" class="auth-form">
        <label>Email</label>
        <input type="email" name="email" placeholder="example@email.com" required />

        <button type="submit" class="login-btn">Send Reset Link</button>
        <p class="switch-text">
            <a href="/JobSeeker/Account/Login">Back to Login</a>
        </p>
    </form>
</div>

@section Scripts {
    <script>
        const message = '@TempData["Message"]';
        if (message && message.length > 0) {
            Swal.fire({
                icon: message.includes("sent") ? "success" : "info",
                title: "Notification",
                text: message,
                confirmButtonColor: "#007bff"
            });
        }
    </script>
}
