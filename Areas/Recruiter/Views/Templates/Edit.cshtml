@* /Areas/Recruiter/Views/Templates/Edit.cshtml *@
@model JobPortal.Areas.Recruiter.Models.TemplateFormVM

<a asp-area="Recruiter" asp-controller="Templates" asp-action="Index" class="d-inline-block text-decoration-none mb-3">
    &leftarrow; Back to Templates
</a>

<div class="card shadow-sm">
    <div class="card-header bg-white fw-semibold">Edit Template</div>
    <div class="card-body">
        <form asp-area="Recruiter" asp-controller="Templates" asp-action="Edit" method="post" class="vstack gap-3">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="TemplateId" />

            <div class="mb-1">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            </div>

            <div>
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" required maxlength="120" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="Subject" class="form-label"></label>
                <span class="text-muted ms-1">(optional)</span>
                <input asp-for="Subject" class="form-control" maxlength="160" />
                <span asp-validation-for="Subject" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="Body" class="form-label"></label>
                <textarea
                    asp-for="Body"
                    class="form-control"
                    rows="8"
                    required
                    data-bs-toggle="tooltip"
                    data-bs-placement="right"
                    data-bs-html="true"
                    title="You can use placeholders like <code>{{FirstName}}</code> or add new ones with <code>{{ }}</code>.">
                </textarea>
                <span asp-validation-for="Body" class="text-danger"></span>
            </div>

            <div class="d-flex gap-2">
                <a asp-area="Recruiter" asp-controller="Templates" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        // Why: ensure tooltip binds reliably
        document.addEventListener('DOMContentLoaded', function () {
            var el = document.getElementById('Body'); // razor generates id from asp-for
            if (el && window.bootstrap) {
                new bootstrap.Tooltip(el);
            }
        });
    </script>
}
