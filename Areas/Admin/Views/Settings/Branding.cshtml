<div class="card shadow-sm">
  <div class="card-header bg-white fw-semibold">Branding</div>
  <div class="card-body">
    <div class="mb-3">
      <label class="form-label">Logo (mock)</label>
      <input class="form-control" type="file" disabled />
    </div>
    <div class="mb-3">
      <label class="form-label">Primary colour</label>
      <input class="form-control form-control-color" type="color" value="#0d6efd" disabled />
    </div>
    <button class="btn btn-primary" disabled>Save (mock)</button>
  </div>
</div>
